{
  "name": "donations-static",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --base=./ && node post-build.js",
    "preview": "vite preview",
    "deploy": "npm run build && rsync -av dist/ ../spyder/spyder/plugins/application/widgets/appeal_page/"
  },
  "devDependencies": {
    "vite": "^8.0.0"
  }
}
